// TEMP VARIABLES
var dir = "up";
var speed = 2;
var wait = 1;

// SPAWN NOTES
function spawnNote(direction){
    var arrow = document.createElement()
    if(direction == "up"){

    }else if(direction == "down"){

    }else if(direction == "left"){

    }else{

    }
}
// MOVE NOTES
